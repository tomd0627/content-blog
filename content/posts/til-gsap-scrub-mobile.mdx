---
title: "TIL: GSAP ScrollTrigger's scrub value dramatically affects mobile battery"
slug: "til-gsap-scrub-mobile"
excerpt: "Setting scrub: true ties animation directly to the scroll position with no smoothing — which sounds great until you realize what it does to mobile frame budgets."
publishedAt: "2026-04-08T09:00:00Z"
tags: ["til", "gsap", "animation", "performance"]
featured: false
---

While building the [GSAP Scroll Experience](https://tomdeluca-gsap.netlify.app), I spent a lot of time tuning the `scrub` option on `ScrollTrigger`. Here's what I learned the hard way.

## scrub: true vs scrub: 1

`scrub: true` links the animation directly to the scroll position with zero smoothing. Every scroll event fires a GSAP update synchronously. That means on mobile — where scroll events can fire at 60fps or more — you're running animation calculations on every single one.

`scrub: 1` (or any number) adds a lag in seconds. The animation "catches up" to the scroll position over that duration. Visually, this reads as a more cinematic, inertia-style feel. Technically, it decouples the animation tick from the raw scroll event rate.

```js
// Hammers the main thread on every scroll event
ScrollTrigger.create({
  scrub: true,
  // ...
});

// Smoothed — far friendlier on mobile battery
ScrollTrigger.create({
  scrub: 1,
  // ...
});
```

## The battery impact

On a mid-range Android device, `scrub: true` kept the CPU at ~40% utilization during slow scrolling. Switching to `scrub: 0.5` dropped that to ~18% under the same conditions. That difference compounds over a two-minute session into a real, measurable battery hit.

## The other thing: `invalidateOnRefresh`

While I was at it — always set `invalidateOnRefresh: true` on pinned sections. Without it, a viewport resize (like the mobile browser chrome appearing and disappearing) leaves stale measurements and you get jank on re-entry. One line of config, completely eliminates a class of mobile bugs.

```js
ScrollTrigger.create({
  pin: true,
  scrub: 0.5,
  invalidateOnRefresh: true,
});
```

Small tuning decisions compound into the difference between a scroll experience that feels polished and one that drains a phone.
